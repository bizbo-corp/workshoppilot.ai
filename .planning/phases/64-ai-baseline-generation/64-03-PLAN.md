---
phase: 64-ai-baseline-generation
plan: 03
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - src/components/journey-flow/scope-chooser.tsx
  - src/components/journey-flow/journey-flow-toolbar.tsx
  - src/components/journey-flow/journey-flow-node-card.tsx
autonomous: true
requirements: [GEN-01, GEN-04, GEN-05]

must_haves:
  truths:
    - "ScopeChooser presents two explicit options — 'Test the whole journey' and 'Test a single feature' — with a concept picker revealed only in single-feature mode"
    - "Generate CTA is disabled until a valid selection exists (feature mode requires a picked concept); a 'start from scratch' escape hatch remains"
    - "Toolbar exposes a Regenerate button (and archetype badge) once a baseline exists, hidden for read-only users"
    - "Annotation nodes render visually distinct from screen cards (dashed border, info icon, no UI-type badge, no connection handles)"
  artifacts:
    - path: "src/components/journey-flow/scope-chooser.tsx"
      provides: "Entry-state scope choice UI + concept picker + generate CTA + escape hatch"
      exports: ["ScopeChooser"]
      min_lines: 80
    - path: "src/components/journey-flow/journey-flow-toolbar.tsx"
      provides: "Regenerate button + archetype badge added to JourneyFlowToolbar"
      contains: "onRegenerate"
    - path: "src/components/journey-flow/journey-flow-node-card.tsx"
      provides: "isAnnotation render variant"
      contains: "isAnnotation"
  key_links:
    - from: "src/components/journey-flow/scope-chooser.tsx"
      to: "src/lib/journey-flow/types.ts"
      via: "TestScope type import"
      pattern: "TestScope"
    - from: "src/components/journey-flow/journey-flow-node-card.tsx"
      to: "src/lib/journey-flow/types.ts"
      via: "isAnnotation flag on JourneyFlowNode data"
      pattern: "isAnnotation"
---

<objective>
Build the three presentational pieces of Phase 64: the ScopeChooser entry state (GEN-01), the toolbar Regenerate button + archetype badge (GEN-04), and the annotation-node render variant (GEN-05 display). Pure components against the Plan 64-01 contracts — wiring happens in Plan 64-04.

Purpose: Parallel-safe UI work; no file overlap with the API route plan.
Output: New scope-chooser component; extended toolbar and node card.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-ai-baseline-generation/64-CONTEXT.md
@src/components/journey-flow/journey-flow-toolbar.tsx
@src/components/journey-flow/journey-flow-node-card.tsx
@src/lib/journey-flow/types.ts
@src/components/ui/icon.tsx
</context>

<interfaces>
From Plan 64-01:
```typescript
// src/lib/journey-flow/types.ts
export type TestScope = 'journey' | 'feature';
export type FlowArchetype = 'linear-sequence' | 'hub-and-spoke' | 'single-page-sections' | 'funnel' | 'branching' | 'single-screen-tool' | 'loop';
export const ARCHETYPE_LABELS: Record<FlowArchetype, string>;
// JourneyFlowNode now has isAnnotation?: boolean
```

Existing toolbar props (extend, don't break — `journey-flow-canvas.tsx` renders this):
```typescript
export interface JourneyFlowToolbarProps {
  sessionId: string;
  isReadOnly?: boolean;
  isApproved: boolean;
  isApproving?: boolean;
  onApprove: () => void;
}
```

Node card data type (extend via the JourneyFlowNode spread — no structural change needed):
```typescript
export type JourneyFlowNodeData = JourneyFlowNode & { onOpenDetail?: ...; onAddNodeAt?: ...; [key: string]: unknown };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ScopeChooser component</name>
  <files>src/components/journey-flow/scope-chooser.tsx</files>
  <action>
Create `src/components/journey-flow/scope-chooser.tsx` (`'use client'`):

```typescript
export interface ScopeChooserConcept { name: string; elevatorPitch?: string; }
export interface ScopeChooserProps {
  concepts: ScopeChooserConcept[];
  isGenerating: boolean;
  onGenerate: (scope: TestScope, selectedConceptName?: string) => void;
  onStartFromScratch: () => void;
}
export function ScopeChooser(props: ScopeChooserProps): JSX.Element;
```

Layout (full content-area entry state, centered, max-w-2xl — replaces the Phase 63 empty state; visual language matches it: `Heading`/`Text` from `@/components/ui/typography`, `Icon`, `Button`, `cn`):

1. Header: workflow icon in `bg-primary/10` circle (same as the Phase 63 empty state), `Heading level={2}` "How do you want to test your idea?", muted `Text` explaining the AI will generate a starting flow they can edit.
2. Two selectable option cards side by side (grid-cols-1 sm:grid-cols-2), acting as a radio group:
   - **Test the whole journey** — icon `workflow` (or `map` if registered), copy: "Map the end-to-end flow a user takes through your product."
   - **Test a single feature** — icon `target`/`focus` style (use an icon already registered in `src/components/ui/icon.tsx`; check the registry first and reuse — only add a new phosphor registration if nothing fits, following the existing pattern in that file), copy: "Test one concept with a short entry → action → result flow."
   - Selected state: `ring-2 ring-selection` + `bg-primary/5`; unselected: `border-border bg-card hover:bg-accent/50`. NO hardcoded gray/blue/white — olive tokens only. Cards are `<button type="button" role="radio" aria-checked={...}>` for accessibility.
   - If `concepts.length === 0`: the single-feature card is disabled (reduced opacity, `aria-disabled`, not selectable) with a small hint "Complete Step 9 (Concept) to test a single feature."
3. When 'feature' is selected: reveal a concept picker below the cards — a vertical list of selectable rows (concept `name` bold + `elevatorPitch` line-clamped muted text), same selected/unselected token treatment, `role="radio"` semantics. Selecting a concept stores its `name` (concept name IS the stable id).
4. CTA row: primary `Button` "Generate baseline flow" with a sparkle/wand icon if registered (else `plus`); disabled when `isGenerating`, when no scope picked, or when scope='feature' without a concept picked. While `isGenerating`: spinner icon (`Icon name="spinner"` with `animate-spin`) + label "Generating your flow…".
5. Escape hatch: small text button below CTA — "Or start from scratch with a blank canvas" — calls `onStartFromScratch`; styled `text-sm text-muted-foreground hover:text-foreground` (same as the Phase 63 back-link).

Keep this component purely presentational: no fetch, no store access — the parent passes callbacks (wired in Plan 64-04). Default local state: no scope preselected (the choice must be explicit — GEN-01).
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -nE "gray-|blue-|bg-white|text-white" src/components/journey-flow/scope-chooser.tsx && echo TOKENS_OK && grep -q "aria-checked" src/components/journey-flow/scope-chooser.tsx && grep -q "disabled" src/components/journey-flow/scope-chooser.tsx && echo BEHAVIOR_PRESENT</automated>
  </verify>
  <done>ScopeChooser compiles; explicit two-option choice with feature-mode concept picker, disabled-state rules, generating state, and start-from-scratch link; olive tokens only; aria-checked radio semantics and disabled-gating mechanically present (BEHAVIOR_PRESENT printed).</done>
</task>

<task type="auto">
  <name>Task 2: Toolbar Regenerate button + archetype badge; annotation node variant</name>
  <files>src/components/journey-flow/journey-flow-toolbar.tsx, src/components/journey-flow/journey-flow-node-card.tsx</files>
  <action>
**A. `journey-flow-toolbar.tsx`** — extend `JourneyFlowToolbarProps` with optional fields (backwards compatible; canvas wiring lands in Plan 64-04):
```typescript
onRegenerate?: () => void;
isGenerating?: boolean;
archetype?: FlowArchetype;
```
In `JourneyFlowToolbar`, after the divider and BEFORE the Mark complete button:
- When `!isReadOnly && onRegenerate`: render a `Button size="sm" variant="outline"` (h-7 text-xs gap-1.5, matching Mark complete sizing) labeled "Regenerate" with a refresh-style icon. Check `src/components/ui/icon.tsx` for a registered refresh/arrows icon (e.g. `refresh`, `arrow-clockwise`, `arrows-clockwise`); if none exists, register one following the file's existing phosphor-icon pattern. While `isGenerating`: disable it and swap icon for `spinner` + `animate-spin`.
- When `archetype` is set: render a small muted badge after the buttons — `span` with `rounded-full border px-2 py-0.5 text-[10px] font-medium bg-muted text-muted-foreground` showing `ARCHETYPE_LABELS[archetype]` (import from `@/lib/journey-flow/types`). This makes the AI's archetype decision visible (supports phase success criterion 3 verification).
- Do not alter existing approve/approved rendering.

**B. `journey-flow-node-card.tsx`** — annotation render variant. At the top of the `JourneyFlowNodeCard` render, branch on `data.isAnnotation`:
- Annotation variant: `w-[300px] rounded-lg border border-dashed bg-muted/60 px-3 py-2.5` with NO left accent border, NO UI-type badge, NO connection handles, NO hover (+) add buttons, and NO `onOpenDetail` click handler (it's an informational note, not an editable screen). Content: header row with `Icon name="info"` (check registry; register if missing) + `data.name` in `text-xs font-semibold text-muted-foreground uppercase tracking-wide`, then `data.purpose` as `text-xs text-muted-foreground` (NOT line-clamped — the full explanation must be readable; GEN-05's "says so in the generated baseline" lives here).
- Selected state still shows `ring-2 ring-selection` (so it can be selected and deleted via existing canvas delete handling — users may dismiss the note).
- Screen-card branch: unchanged.
Olive tokens only — no amber/yellow palette hardcodes; the dashed border + muted background is the distinct treatment.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "onRegenerate" src/components/journey-flow/journey-flow-toolbar.tsx && grep -q "isAnnotation" src/components/journey-flow/journey-flow-node-card.tsx && ! grep -nE "gray-|blue-|bg-white|amber-|yellow-" src/components/journey-flow/journey-flow-toolbar.tsx src/components/journey-flow/journey-flow-node-card.tsx && echo WIRED</automated>
  </verify>
  <done>Toolbar accepts onRegenerate/isGenerating/archetype and renders the button + badge; node card renders annotation nodes as dashed muted notes without handles or edit affordances; existing behavior untouched (props optional).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `grep -n "role=\"radio\"" src/components/journey-flow/scope-chooser.tsx` — explicit-choice semantics present
- `grep -nE "gray-|blue-|bg-white|amber-|yellow-" src/components/journey-flow/scope-chooser.tsx src/components/journey-flow/journey-flow-node-card.tsx src/components/journey-flow/journey-flow-toolbar.tsx` returns nothing (all three modified files token-clean)
- Existing usages still compile: `journey-flow-canvas.tsx` renders `JourneyFlowToolbar` with only the original 5 props (new props optional)
</verification>

<success_criteria>
- ScopeChooser is drop-in ready for Plan 64-04's `!hasNodes` branch — props-only, no data dependencies
- Regenerate affordance and archetype badge exist behind optional props without breaking Phase 63 rendering
- Annotation nodes are visually unmistakable as notes, satisfying the display half of GEN-05
</success_criteria>

<output>
After completion, create `.planning/phases/64-ai-baseline-generation/64-03-SUMMARY.md`
</output>
