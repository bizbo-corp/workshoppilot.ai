---
phase: 66-low-fi-prototype-prompt
plan: 02
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/page.tsx
  - src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/prototype-prompt-content.tsx
autonomous: true
requirements: [PROMPT-01, PROMPT-03, PROMPT-05]

must_haves:
  truths:
    - "User sees a segmented fidelity switch: 'Low fidelity' active, 'High fidelity' visible with a 'Coming later' badge, muted styling, not-allowed cursor, and zero interaction"
    - "User clicks 'Generate prompt' and after a loading state the prompt appears as a collapsed ~10-line preview with a 'Show full prompt' expand control"
    - "Copy button always copies the FULL prompt text, morphs to 'Copied ✓' for ~2s, and fires a Sonner success toast"
    - "Next-step instructions read 'Paste into your preferred AI coding agent — v0, Claude, Codex, Replit' with a help link that opens an in-app dialog (3-step how-to + agent links)"
    - "A previously generated prompt survives reload without an AI call; when Journey Flow changed since generation, a stale notice with a Regenerate button appears"
    - "In single-feature mode the card shows the scope label 'Prompt covers your feature mini-flow'"
    - "No v0-specific 'Create on v0' button or create-v0-chat dependency exists anywhere in the new flow"
  artifacts:
    - path: "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/page.tsx"
      provides: "Server component: auth, Journey Flow + Prototype Prompt row loading, staleness computation, redirect guard"
      contains: "Prototype Prompt:"
      min_lines: 80
    - path: "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/prototype-prompt-content.tsx"
      provides: "Client component: fidelity switch, generate/regenerate, preview, copy UX, help dialog"
      exports: ["PrototypePromptContent"]
      min_lines: 150
  key_links:
    - from: "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/prototype-prompt-content.tsx"
      to: "/api/build-pack/generate-prototype-prompt"
      via: "fetch POST on Generate/Regenerate click"
      pattern: "api/build-pack/generate-prototype-prompt"
    - from: "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/page.tsx"
      to: "build_packs table"
      via: "Drizzle like() queries for 'Journey Flow:%' and 'Prototype Prompt:%' rows"
      pattern: "like\\(buildPacks\\.title"
    - from: "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/prototype-prompt-content.tsx"
      to: "src/lib/clipboard.ts"
      via: "copyToClipboard import"
      pattern: "copyToClipboard"
---

<objective>
Replace the Phase 65 placeholder body of `/workshop/[sessionId]/outputs/prototype-prompt` with the real prototype prompt builder: fidelity switch (low active, hi-fi stubbed "coming later"), on-demand generation against Plan 01's API route, persisted-prompt reload, staleness banner with Regenerate, collapsed preview, agent-agnostic copy UX, and the help dialog.

Note on placement: the locked decision says the builder lives in a card consistent with Phase 65 guidance cards; the shipped Phase 65 state established `/outputs/prototype-prompt` as the canonical page the validate-step guidance card gates and links to ("Phase 66 replaces page body only, route already exists" — STATE.md decision). Build the experience as a card-styled page here, reached from the validate page's gated link.

Purpose: PROMPT-01 (fidelity switch), PROMPT-03 (agent-agnostic copy/paste handoff), PROMPT-05 (mini-flow scope label).
Output: Reworked page.tsx server component + new PrototypePromptContent client component.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-low-fi-prototype-prompt/66-CONTEXT.md
@.planning/phases/66-low-fi-prototype-prompt/66-RESEARCH.md
@src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/page.tsx
@src/app/(dashboard)/workshop/[sessionId]/outputs/journey-flow/page.tsx
@src/lib/journey-flow/types.ts
@src/lib/clipboard.ts

<interfaces>
<!-- Contracts from Plan 01 (build against these exactly). -->

From src/lib/ai/prompts/low-fi-prototype-prompt.ts (created in Plan 01):
```typescript
export interface StoredPrototypePrompt {
  promptText: string;
  generatedFromFlowUpdatedAt: string; // ISO — Journey Flow row's updatedAt at generation time
  testScope: TestScope;               // 'journey' | 'feature'
  selectedConceptId?: string;
  generatedAt: string;
}
```

POST /api/build-pack/generate-prototype-prompt (created in Plan 01):
```
Request:  { workshopId: string }
200:      { promptText, buildPackId, generatedFromFlowUpdatedAt, testScope, selectedConceptId, usedLlm }
400:      { error } — Journey Flow missing/empty
401/403:  auth/ownership failures (participants get 403)
```

From src/lib/clipboard.ts:
```typescript
export async function copyToClipboard(text: string): Promise<boolean>; // handles execCommand fallback
```

Icon registry names available in src/components/ui/icon.tsx (use ONLY registered names):
copy, check, sparkles, spinner, refresh, info, help-circle, alert-triangle, arrow-left, layers

Existing page boilerplate to KEEP (placeholder page.tsx): session lookup via db.query.sessions, Clerk auth(), owner check + resolveClerkParticipant for read-only participants, backHref/backLabel from `?from=validate`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework page.tsx server component — load rows, compute staleness, guard</name>
  <files>src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/page.tsx</files>
  <action>
Rework the placeholder page (keep the existing auth/session/ownership boilerplate and the `from`-param back-link logic; track `isReadOnly` the same way journey-flow/page.tsx does — participant resolution sets `isReadOnly = true`).

Add data loading after the auth block:
1. Journey Flow row: `db.select().from(buildPacks).where(and(eq(buildPacks.workshopId, workshop.id), like(buildPacks.title, 'Journey Flow:%')))`; find `formatType === 'json' && content`; `JSON.parse` into `Partial<JourneyFlowState>` (try/catch → null).
2. Guard (Pitfall 3): if no parsed flow state OR `!flowState.nodes?.length` → `redirect(`/workshop/${sessionId}/outputs/journey-flow?from=prototype-prompt`)` — user must build a Journey Flow first.
3. Prototype Prompt row: same query with `like(buildPacks.title, 'Prototype Prompt:%')`; parse content as `StoredPrototypePrompt` (try/catch → null).
4. Staleness (research Pattern 4): `const flowUpdatedAt = journeyFlowRow.updatedAt.toISOString(); const isStale = !!storedPrompt && storedPrompt.generatedFromFlowUpdatedAt !== flowUpdatedAt;`
5. Scope: `const testScope = storedPrompt?.testScope ?? flowState.testScope ?? 'journey';`

Render: page chrome (full-height bg-background, back link with arrow-left icon reusing existing backHref/backLabel) wrapping the client component:

```tsx
<PrototypePromptContent
  sessionId={sessionId}
  workshopId={workshop.id}
  initialPrompt={storedPrompt?.promptText ?? null}
  initialGeneratedFromFlowUpdatedAt={storedPrompt?.generatedFromFlowUpdatedAt ?? null}
  flowUpdatedAt={flowUpdatedAt}
  initialIsStale={isStale}
  testScope={testScope}
  isReadOnly={isReadOnly}
  backHref={backHref}
  backLabel={backLabel}
/>
```

Keep imports olive-token compliant (no gray-*/white classes). Remove the "coming soon" placeholder copy entirely. Update the file-top doc comment: this is now the Phase 66 prototype prompt builder.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Prototype Prompt:" "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/page.tsx" && grep -q "journey-flow?from=prototype-prompt" "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/page.tsx" && echo OK</automated>
  </verify>
  <done>Page loads both build_pack rows, redirects to journey-flow when no flow exists, computes isStale via timestamp comparison, passes typed props to PrototypePromptContent; placeholder copy gone.</done>
</task>

<task type="auto">
  <name>Task 2: Create PrototypePromptContent client component — switch, generate, preview, copy, help</name>
  <files>src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/prototype-prompt-content.tsx</files>
  <action>
Create `'use client'` component `PrototypePromptContent` with the props from Task 1. Layout: centered column, `max-w-2xl`, card container `rounded-2xl border border-border bg-card p-6 sm:p-8 shadow-sm` (consistent with Phase 65 guidance-card styling), serif h1 "Prototype prompt builder" + one-line description.

State: `promptText` (init from `initialPrompt`), `generatedFromFlowUpdatedAt`, `isStale` (init `initialIsStale`), `isGenerating`, `copied`, `expanded`, `helpOpen`.

**Fidelity switch (PROMPT-01)** — locked: segmented two-option pill, hi-fi visible but zero interaction:
- Wrapper: `inline-flex items-center gap-1 rounded-full border border-border bg-muted/40 p-1` with `role="radiogroup"` and an aria-label.
- "Low fidelity": `<button type="button" aria-pressed="true">` styled active (`rounded-full bg-card px-4 py-1.5 text-sm font-medium text-foreground shadow-sm`). It is the only option — no state needed; clicking is a no-op.
- "High fidelity": `<span aria-disabled="true">` (NOT a button — zero interaction) styled `rounded-full px-4 py-1.5 text-sm text-muted-foreground/60 cursor-not-allowed select-none` containing the label plus a small badge `<span className="ml-1.5 rounded-full bg-muted px-2 py-0.5 text-[10px] font-medium uppercase tracking-wide text-muted-foreground">Coming later</span>`.

**Scope label (PROMPT-05)** — when `testScope === 'feature'`, render immediately below the switch (before the Generate button, per research OQ3): small muted line with `info` icon: "Prompt covers your feature mini-flow".

**Generate / loading / stale states:**
- No prompt yet: explanation paragraph ("Turn your Journey Flow into a ready-to-paste prototype prompt…") + primary Button "Generate prompt" with `sparkles` icon. Hidden when `isReadOnly` (show muted text "Only the workshop owner can generate the prompt." instead; read-only users can still view/copy an existing prompt).
- `handleGenerate`: `setIsGenerating(true)`; `fetch('/api/build-pack/generate-prototype-prompt', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ workshopId }) })`. On ok: set `promptText`, `generatedFromFlowUpdatedAt` from response, `setIsStale(false)`, `setExpanded(false)`. On !ok or thrown: `toast.error('Failed to generate prompt', { description: 'Please try again.' })`. Finally `setIsGenerating(false)`.
- While generating: button disabled showing `spinner` icon with `animate-spin` + "Generating prompt…"; keep prior prompt (if any) visible but dimmed (`opacity-50 pointer-events-none` on the preview block).
- Stale notice (locked copy): when `isStale && promptText`, render above the preview a notice box `rounded-xl border border-border bg-muted/40 px-4 py-3 text-sm` with `alert-triangle` icon: "Journey Flow has changed since this prompt was generated." plus a secondary Button "Regenerate" (`refresh` icon) calling `handleGenerate` (route always overwrites — no version history). Hide the Regenerate button when `isReadOnly`. When NOT stale and a prompt exists, still show a small tertiary "Regenerate" button near the preview footer (owner only) so users can refresh deliberately.

**Prompt preview (locked: collapsed ~10 lines, expand control, copy always copies full text):**
- Render `promptText` in `<pre className="whitespace-pre-wrap break-words rounded-xl border border-border bg-muted/30 p-4 font-mono text-xs text-foreground/90">`.
- `const lines = promptText.split('\n')`; when `!expanded && lines.length > 10` show only `lines.slice(0, 10).join('\n')` with a bottom fade and a "Show full prompt" text button; when expanded, full text + "Collapse" button. Only show the expand control when `lines.length > 10` (Pitfall 6). Reset `expanded` to false whenever promptText changes (already done in handleGenerate; also add `useEffect` keyed on `promptText` as a guard).
- Copy button (prominent, primary, above or beside the preview): `handleCopy` uses `copyToClipboard(promptText)` (FULL text regardless of collapse). On success: `setCopied(true)`, `toast.success('Prompt copied', { description: 'Paste into your preferred AI coding agent.' })`, `setTimeout(() => setCopied(false), 2000)`. Button content morphs: default `copy` icon + "Copy prompt" → copied state `check` icon + "Copied ✓". On failure: `toast.error('Copy failed')`.

**Next-step instructions + help link (PROMPT-03):** below the copy button render exactly: "Paste into your preferred AI coding agent — v0, Claude, Codex, Replit" as muted text, followed by a text button "How do I use this?" (`help-circle` icon) opening a shadcn `Dialog` (`helpOpen` state). Dialog content: title "Use your prototype prompt", ordered 3-step list — 1. Copy the prompt above. 2. Open your preferred AI coding agent. 3. Paste the prompt and run it — you'll get a clickable wireframe prototype. Then a row of external links (each `target="_blank" rel="noopener noreferrer"`, underlined muted style): v0 → https://v0.dev, Claude → https://claude.ai, Codex → https://chatgpt.com/codex, Replit → https://replit.com. No separate docs page, no post-copy tracking, and absolutely NO "Create on v0" action or `create-v0-chat` import.

Use `Icon` from `@/components/ui/icon` with only registered names (copy, check, sparkles, spinner, refresh, info, help-circle, alert-triangle), `Button` and `Dialog` from `@/components/ui/*`, `toast` from `sonner`. Olive tokens only — no gray-*/blue-*/white hardcodes; verify dark mode classes follow existing card patterns.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint -- --quiet 2>/dev/null; grep -q "copyToClipboard" "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/prototype-prompt-content.tsx" && grep -q "Coming later" "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/prototype-prompt-content.tsx" && ! grep -rq "create-v0-chat\|v0.dev/chat/api" "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/" && echo OK</automated>
  </verify>
  <done>Client component renders fidelity switch with stubbed hi-fi, on-demand generation with loading state, collapsed preview with conditional expand, full-text copy with morph + toast, locked next-step copy with help dialog (3 steps + 4 agent links), stale notice + Regenerate, feature-mode scope label, read-only handling; no v0-specific dependencies.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` and `npm run lint` pass.
- `grep -rn "create-v0-chat" "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/"` returns nothing.
- Page redirects to journey-flow when no Journey Flow row exists (code-level check: redirect call present behind the empty-flow guard).
- Stale computation compares `StoredPrototypePrompt.generatedFromFlowUpdatedAt` against the Journey Flow row's `updatedAt` ISO string.
</verification>

<success_criteria>
- Fidelity switch matches locked decisions: low active, hi-fi "Coming later" badge, muted, not-allowed cursor, zero interaction (PROMPT-01).
- Copy/paste handoff is agent-agnostic with prominent copy, morph + toast feedback, locked instruction copy, and in-dialog help (PROMPT-03).
- Persisted prompt loads from DB on reload without AI calls; staleness banner and Regenerate behave per locked decisions.
- Feature-mode scope label renders (PROMPT-05 UI half).
</success_criteria>

<output>
After completion, create `.planning/phases/66-low-fi-prototype-prompt/66-02-SUMMARY.md`
</output>
