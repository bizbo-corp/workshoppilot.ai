---
phase: 38-pricing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/pricing/page.tsx
  - src/proxy.ts
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-03, PRICE-04]

must_haves:
  truths:
    - "Visiting /pricing shows three tiers: Single Use, Facilitator, and Annual Subscription"
    - "Each tier displays name, price point, feature list, and a CTA button"
    - "The pricing page does not appear in the main navigation, landing header, sidebar, or any persistent nav element"
    - "Pricing content reflects real differentiated tiers with specific feature differences between plans"
    - "Unauthenticated users can access /pricing without redirect"
  artifacts:
    - path: "src/app/pricing/page.tsx"
      provides: "Pricing page with three-tier layout"
      contains: "Single Use"
    - path: "src/proxy.ts"
      provides: "Public route matcher includes /pricing"
      contains: "/pricing"
  key_links:
    - from: "src/proxy.ts"
      to: "/pricing"
      via: "isPublicRoute matcher"
      pattern: "/pricing"
---

<objective>
Create the /pricing page with three differentiated tiers (Single Use, Facilitator, Annual Subscription), each showing name, price, feature list, and CTA button. Make the route publicly accessible but hidden from all navigation.

Purpose: Give anyone with the direct URL real pricing expectations without cluttering the app navigation or implying payment processing exists.
Output: Static pricing page at /pricing, publicly accessible, not linked from nav.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/components/landing/value-props-section.tsx
@src/proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pricing page with three-tier layout and real content</name>
  <files>src/app/pricing/page.tsx</files>
  <action>
Create `src/app/pricing/page.tsx` as a server component (no 'use client') following the Phase 37 landing page pattern.

**Pattern to follow:** Same as `src/components/landing/value-props-section.tsx` — typed data array at module level for content/presentation separation.

**Structure:**
1. Define a `PricingTier` interface: `{ name: string; price: string; period: string; description: string; features: string[]; cta: string; highlighted?: boolean }`
2. Define `PRICING_TIERS: PricingTier[]` at module level with three tiers:

**Tier 1 — Single Use ($9)**
- "One complete workshop session"
- Features: AI-guided 10-step design thinking process, Complete Build Pack output (PRD, user stories, tech specs), Canvas whiteboard with visual ideation tools, Export-ready deliverables
- CTA: "Start a Workshop"

**Tier 2 — Facilitator ($29/month)** [highlighted: true]
- "For professionals running multiple workshops"
- Features: Everything in Single Use, Unlimited workshops per month, Workshop history and management, Priority AI processing, Save and resume any workshop
- CTA: "Get Facilitator"

**Tier 3 — Annual Subscription ($249/year)**
- "Best value for ongoing use"
- Features: Everything in Facilitator, 2 months free (vs. monthly), Early access to new features, Team collaboration (coming soon), Custom workshop templates (coming soon)
- CTA: "Subscribe Annually"

**Layout and styling — olive theme tokens throughout, zero hardcoded colors:**
- Page wrapper: `min-h-screen bg-background`
- Reuse LandingHeader (sticky frosted glass) and Footer from landing page for visual consistency
- Import: `import { LandingHeader } from '@/components/layout/landing-header'` and `import { Footer } from '@/components/landing/footer'`
- Section: `py-20 sm:py-24 bg-background`
- Container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8`
- Heading: `text-3xl sm:text-4xl font-bold text-center text-foreground mb-4`
- Subheading: `text-muted-foreground text-center mb-12 sm:mb-16 max-w-2xl mx-auto`
- Grid: `grid grid-cols-1 md:grid-cols-3 gap-6 sm:gap-8`
- Each tier card: `rounded-xl border border-border bg-card p-6 sm:p-8 flex flex-col`
- Highlighted tier: add `ring-2 ring-olive-600 dark:ring-olive-400` and a "Most Popular" badge (`text-xs font-semibold uppercase tracking-wider text-olive-700 dark:text-olive-300 bg-olive-100 dark:bg-olive-900 rounded-full px-3 py-1`)
- Tier name: `text-xl font-bold text-foreground`
- Price: `text-3xl sm:text-4xl font-bold text-foreground` with period in `text-sm text-muted-foreground`
- Description: `text-sm text-muted-foreground`
- Features list: Use `Check` icon from lucide-react (`import { Check } from 'lucide-react'`), icon color `text-olive-600 dark:text-olive-400`, feature text `text-sm text-foreground`
- CTA button: Use a `<button>` styled as `w-full rounded-lg py-3 px-4 font-semibold text-sm transition-colors`. For highlighted tier: `bg-olive-700 text-white hover:bg-olive-800 dark:bg-olive-600 dark:hover:bg-olive-500`. For others: `bg-card border border-border text-foreground hover:bg-background`
- Add a note below the grid: `text-sm text-muted-foreground text-center mt-12` — "Pricing is for informational purposes. Payment processing coming soon."

**DO NOT:**
- Add 'use client' — this is a pure server component
- Link to this page from any navigation component
- Import or use any payment/billing libraries
- Use hardcoded color values (use only theme tokens and olive-* scale)
  </action>
  <verify>
1. `ls src/app/pricing/page.tsx` — file exists
2. `npx tsc --noEmit --pretty 2>&1 | head -30` — no TypeScript errors in pricing page
3. Grep for hardcoded colors: `grep -n 'bg-white\|bg-gray\|text-gray\|text-blue\|bg-blue' src/app/pricing/page.tsx` — should return nothing
4. Grep for 'use client': `grep "'use client'" src/app/pricing/page.tsx` — should return nothing
  </verify>
  <done>
- /pricing page.tsx exists as a server component with zero hardcoded colors
- Three tiers rendered (Single Use $9, Facilitator $29/mo, Annual $249/yr) with names, prices, feature lists, and CTA buttons
- Facilitator tier visually highlighted as "Most Popular"
- Informational disclaimer shown below tiers
- LandingHeader and Footer reused for visual consistency
  </done>
</task>

<task type="auto">
  <name>Task 2: Add /pricing to public route matcher in middleware</name>
  <files>src/proxy.ts</files>
  <action>
In `src/proxy.ts`, add `'/pricing'` to the `isPublicRoute` createRouteMatcher array so unauthenticated users can access the pricing page without being redirected.

Add it after the `'/'` entry:
```
const isPublicRoute = createRouteMatcher([
  '/',
  '/pricing',
  '/sign-in(.*)',
  ...
]);
```

**Verify PRICE-03 (hidden from nav):** Confirm that NO navigation component links to /pricing:
- `src/components/layout/landing-header.tsx` — no pricing link
- `src/components/layout/workshop-header.tsx` — no pricing link
- `src/components/layout/workshop-sidebar.tsx` — no pricing link

These files should NOT be modified. PRICE-03 is satisfied by absence — the route exists but no nav element links to it.
  </action>
  <verify>
1. `grep "pricing" src/proxy.ts` — shows /pricing in public route matcher
2. `grep -rn "pricing" src/components/layout/` — should return nothing (not in any nav)
3. `npx tsc --noEmit --pretty 2>&1 | head -30` — no TypeScript errors
  </verify>
  <done>
- /pricing is in the public route matcher — unauthenticated visitors are not redirected
- No navigation component (landing header, workshop header, workshop sidebar) contains a link to /pricing
- PRICE-03 verified: page is reachable only via direct URL
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit`
2. Targeted lint passes: `npx next lint --file src/app/pricing/page.tsx`
3. No hardcoded colors in pricing page
4. No 'use client' in pricing page (server component)
5. /pricing in public route matcher
6. No pricing link in any navigation component
7. Build check: `npm run build 2>&1 | grep -E "pricing|error"` confirms page compiles
</verification>

<success_criteria>
- Visiting /pricing renders three pricing tiers with real content
- Each tier shows name, price, feature list, and CTA button
- Facilitator tier is visually highlighted as "Most Popular"
- Page uses olive theme tokens throughout (zero hardcoded colors)
- Page is accessible to unauthenticated users (public route)
- No navigation element links to /pricing
- Informational disclaimer clarifies no payment processing yet
</success_criteria>

<output>
After completion, create `.planning/phases/38-pricing-page/38-01-SUMMARY.md`
</output>
