---
status: complete
phase: 58-facilitator-controls
source: 58-UAT.md (gap fix retest), guest-outputs-access commit
started: 2026-02-28T13:00:00Z
updated: 2026-02-28T13:09:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Facilitator End Session Redirect (Gap Fix Retest)
expected: Facilitator clicks "End Session" → confirms → redirected to /dashboard (the workshop listing page). Page loads successfully — no 404.
result: pass

### 2. Guest Sees "View Workshop Outputs" Button
expected: After the facilitator ends the session, the guest participant sees a full-screen overlay with "View Workshop Outputs" button (NOT "Return to Dashboard").
result: pass

### 3. Guest Navigates to Outputs Page
expected: Guest clicks "View Workshop Outputs" → navigates to /workshop/{sessionId}/outputs. The page loads successfully showing the Build Pack with the workshop title.
result: pass

### 4. Guest Read-Only — No "Back to Workshop" Link
expected: On the outputs page, the guest does NOT see a "Back to Workshop" link at the top. Only the Build Pack header and cards are visible.
result: pass

### 5. Guest Read-Only — Generated Deliverables Viewable
expected: If a PRD or Tech Specs has been generated, the guest can click the card to view details, copy content, and see format pills. The "View Details" button works normally.
result: pass

### 6. Guest Read-Only — Ungenerated Deliverables Disabled
expected: For deliverables that haven't been generated yet (PRD or Tech Specs), the guest sees a disabled "Not yet generated" button instead of a "Generate" button.
result: pass

### 7. Guest Cookie Validation — Wrong Workshop Rejected
expected: If a guest clears their wp_guest cookie (or uses a cookie from a different workshop) and navigates directly to the outputs URL, they are redirected to / (the home page).
result: pass

### 8. Guest Cookie Validation — No Cookie Rejected
expected: In an incognito/private window (no wp_guest cookie), navigating directly to /workshop/{sessionId}/outputs redirects to / (the home page).
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
