---
phase: 36-olive-theme-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/header.tsx
  - src/components/auth/auth-wall-modal.tsx
  - src/components/auth/sign-in-modal.tsx
  - src/components/auth/sign-up-modal.tsx
  - src/app/dashboard/page.tsx
  - src/components/dashboard/workshop-card.tsx
  - src/app/admin/page.tsx
autonomous: true

must_haves:
  truths:
    - "Header sign-in button uses olive primary color, not blue-600"
    - "Auth wall modal uses olive palette for all text, backgrounds, accent circles, and links — no gray-* or blue-* classes"
    - "Dashboard 'Continue where you left off' card uses olive accent, not blue-50/blue-200"
    - "Admin page uses theme tokens (bg-background, text-foreground) instead of hardcoded gray-* classes"
    - "Workshop card hover states use olive tones, not blue-600 or gray-700"
    - "All sign-in/sign-up modal text and links use olive/muted-foreground instead of gray-600/blue-600"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Olive-themed header with themed sign-in button"
    - path: "src/components/auth/auth-wall-modal.tsx"
      provides: "Auth wall with olive palette throughout"
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard with olive accent card"
    - path: "src/app/admin/page.tsx"
      provides: "Admin page using theme tokens"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "globals.css"
      via: "CSS variable tokens (bg-primary, text-primary-foreground)"
      pattern: "bg-primary"
    - from: "src/app/dashboard/page.tsx"
      to: "globals.css"
      via: "olive accent classes replacing blue-50/blue-200"
      pattern: "bg-olive|bg-accent|border-olive"
---

<objective>
Replace all hardcoded gray/blue/white/zinc Tailwind classes in app-level surfaces (header, auth modals, dashboard, admin page, workshop cards) with olive theme tokens or neutral-olive utility classes.

Purpose: Eliminate non-olive colors from the app shell so every surface reads as a single cohesive product with the "consultant authority" olive aesthetic.

Output: 7 files updated — no gray-*, blue-*, bg-white, or dark:bg-zinc-* classes remain in app surface components.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme header, auth modals, and sign-in/sign-up links</name>
  <files>
    src/components/layout/header.tsx
    src/components/auth/auth-wall-modal.tsx
    src/components/auth/sign-in-modal.tsx
    src/components/auth/sign-up-modal.tsx
  </files>
  <action>
Replace all hardcoded color classes with olive theme equivalents in these 4 files:

**header.tsx:**
- `bg-white` → `bg-card` (uses neutral-olive-50 in light, neutral-olive-900 in dark)
- `bg-blue-600` → `bg-primary` on sign-in button
- `text-white` (on button) → `text-primary-foreground`
- `hover:bg-blue-500` → `hover:bg-primary/90`
- `focus:ring-blue-600` → `focus:ring-ring`
- `focus:ring-offset-2` → keep as-is

**auth-wall-modal.tsx (15 replacements):**
- `text-gray-900` → `text-foreground`
- `text-gray-600` → `text-muted-foreground`
- `text-gray-500` → `text-muted-foreground`
- `bg-blue-600 text-white` (circle) → `bg-primary text-primary-foreground`
- `bg-gray-200 text-gray-700` (step circles) → `bg-secondary text-secondary-foreground`
- `text-blue-600 hover:text-blue-500` (links) → `text-primary hover:text-primary/80`
- `text-gray-500 hover:text-gray-700` ("Not now" link) → `text-muted-foreground hover:text-foreground`

**sign-in-modal.tsx and sign-up-modal.tsx (3 replacements each):**
- `text-gray-600` → `text-muted-foreground`
- `text-blue-600 hover:text-blue-500` → `text-primary hover:text-primary/80`
  </action>
  <verify>
Run `grep -n 'text-gray\|bg-gray\|text-blue\|bg-blue\|bg-white' src/components/layout/header.tsx src/components/auth/auth-wall-modal.tsx src/components/auth/sign-in-modal.tsx src/components/auth/sign-up-modal.tsx` — should return zero matches (except any in comments).
  </verify>
  <done>All 4 files use only theme tokens and neutral-olive utility classes. No gray-*, blue-*, or bg-white classes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Theme dashboard page, workshop card, and admin page</name>
  <files>
    src/app/dashboard/page.tsx
    src/components/dashboard/workshop-card.tsx
    src/app/admin/page.tsx
  </files>
  <action>
Replace all hardcoded color classes with olive theme equivalents in these 3 files:

**dashboard/page.tsx:**
- The "Continue where you left off" card: `border-blue-200 bg-blue-50 dark:border-blue-900 dark:bg-blue-950` → `border-olive-200 bg-olive-50 dark:border-olive-900 dark:bg-olive-950` (use the olive-* scale, not neutral-olive, for this accent card since it should have a green tint to stand out from the neutral background)

**workshop-card.tsx:**
- `dark:hover:border-gray-700` → `dark:hover:border-neutral-olive-700`
- `hover:text-blue-600` (on title) → `hover:text-olive-600`

**admin/page.tsx (18 replacements):**
- `bg-gray-50` → `bg-background`
- `text-gray-900` → `text-foreground`
- `text-gray-600` → `text-muted-foreground`
- `text-gray-500` → `text-muted-foreground`
- Apply systematically to all instances. The admin page is a placeholder — use theme tokens throughout: `bg-card` for card backgrounds, `text-foreground` for headings, `text-muted-foreground` for descriptions, `bg-background` for page background.
- `bg-white` (4 card divs) → `bg-card`
  </action>
  <verify>
Run `grep -n 'text-gray\|bg-gray\|text-blue\|bg-blue\|border-gray\|border-blue\|bg-white' src/app/dashboard/page.tsx src/components/dashboard/workshop-card.tsx src/app/admin/page.tsx` — should return zero matches.
  </verify>
  <done>Dashboard continue card uses olive accent. Workshop card hover uses olive. Admin page uses theme tokens throughout. No gray-*/blue-* classes remain in these files.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'bg-gray\|text-gray\|border-gray\|bg-blue\|text-blue\|border-blue\|ring-blue\|bg-white\|bg-zinc\|text-zinc\|border-zinc' src/components/layout/header.tsx src/components/auth/ src/app/dashboard/page.tsx src/components/dashboard/workshop-card.tsx src/app/admin/page.tsx` returns 0 matches
2. `npm run build` succeeds without errors
3. Light mode: header, dashboard, auth modals all show olive tones — no blue or default gray visible
4. Dark mode: same surfaces show dark olive tones — no zinc or default dark backgrounds
</verification>

<success_criteria>
All app-level surfaces (header, auth modals, dashboard, admin) use olive theme tokens. Zero hardcoded gray/blue/white/zinc classes remain in the 7 target files.
</success_criteria>

<output>
After completion, create `.planning/phases/36-olive-theme-rollout/36-01-SUMMARY.md`
</output>
