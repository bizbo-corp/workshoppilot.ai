---
phase: 37-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/layout/landing-header.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/footer.tsx
autonomous: true

must_haves:
  truths:
    - "A first-time visitor sees a hero with headline, subheadline, and a primary CTA button that routes to sign-in or the dashboard"
    - "The navigation includes a visible sign-in or dashboard link that persists across all landing page sections"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page composition — assembles header, hero, content sections, footer"
      contains: "LandingHeader"
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero section with headline, subheadline, CTA buttons"
      min_lines: 30
    - path: "src/components/layout/landing-header.tsx"
      provides: "Sticky navigation header with sign-in CTA"
      contains: "sticky"
    - path: "src/components/landing/footer.tsx"
      provides: "Landing page footer with branding and copyright"
      min_lines: 15
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/landing/hero-section.tsx"
      via: "component import"
      pattern: "import.*HeroSection"
    - from: "src/components/landing/hero-section.tsx"
      to: "src/components/workshop/start-workshop-button.tsx"
      via: "CTA button"
      pattern: "StartWorkshopButton"
    - from: "src/components/layout/landing-header.tsx"
      to: "@clerk/nextjs"
      via: "auth-aware navigation"
      pattern: "SignedIn|SignedOut"
---

<objective>
Restructure the landing page from a minimal centered layout into a full-page marketing scaffold with an enhanced hero section, sticky navigation, and footer.

Purpose: Establish the page structure that subsequent content sections (value props, testimonials) will slot into. The hero becomes the anchor — clear value proposition, strong CTA, professional visual hierarchy using the olive theme.
Output: A landing page with sticky header, hero section, placeholder for content sections, and footer.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/components/layout/landing-header.tsx
@src/components/workshop/start-workshop-button.tsx
@src/components/Logo.tsx
@src/components/ui/button.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract hero section component and enhance with richer content</name>
  <files>src/components/landing/hero-section.tsx</files>
  <action>
Create a new `HeroSection` component that replaces the inline hero content currently in page.tsx. This is a server component (no 'use client' needed) that receives no props.

The hero section should:
- Use a full-viewport-height layout (`min-h-[calc(100vh-4rem)]`) to account for the sticky header height, centered with flexbox
- Display the Logo component at `size="lg"` with the existing `sm:text-7xl md:text-8xl` responsive classes
- Show a headline: "From Vague Idea to AI-Ready Specs" in a larger size than current (`text-3xl sm:text-4xl md:text-5xl font-bold tracking-tight text-foreground`)
- Show a subheadline below: "AI-powered design thinking that turns rough concepts into validated PRDs, user stories, and tech specs — no product management experience required." in `text-lg text-muted-foreground max-w-2xl mx-auto`
- Display the `StartWorkshopButton` as the primary CTA, plus the existing `SignedIn` conditional "Continue Workshop" link to /dashboard using `Button` variant="outline"
- Add a subtle visual cue below the CTA area — a small downward chevron icon (`ChevronDown` from lucide-react) in `text-muted-foreground/50 animate-bounce` to hint at scrollable content below
- Use olive theme tokens throughout: `bg-background`, `text-foreground`, `text-muted-foreground` — NO hardcoded colors
- Container: `mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 text-center`
  </action>
  <verify>File exists at src/components/landing/hero-section.tsx, exports HeroSection, imports Logo, StartWorkshopButton, Button, ChevronDown, and SignedIn from @clerk/nextjs.</verify>
  <done>HeroSection component renders a full-height hero with logo, headline, subheadline, CTA buttons, and scroll indicator using olive theme tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Make landing header sticky and add footer, restructure page.tsx</name>
  <files>src/components/layout/landing-header.tsx, src/components/landing/footer.tsx, src/app/page.tsx</files>
  <action>
**landing-header.tsx changes:**
Make the header sticky so it persists as users scroll through landing page sections:
- Change the `<header>` className from `w-full border-b border-border bg-background` to `sticky top-0 z-40 w-full border-b border-border bg-background/95 backdrop-blur-sm`
- The `bg-background/95 backdrop-blur-sm` gives a frosted glass effect so content scrolling behind is slightly visible
- Keep all existing auth logic (SignedIn/SignedOut, UserButton, Dashboard link, sign-in modal) exactly as-is
- Keep the existing max-w-7xl container and padding

**footer.tsx (new file):**
Create a simple, elegant footer component (server component, no 'use client'):
- Full-width with `border-t border-border bg-background` wrapper
- Inner container: `mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8`
- Left side: Logo at `size="sm"` and a line of text: "AI-powered design thinking for everyone." in `text-sm text-muted-foreground`
- Right side: copyright text `{new Date().getFullYear()} WorkshopPilot` in `text-sm text-muted-foreground`
- Layout: `flex flex-col sm:flex-row items-center justify-between gap-4`
- Export as named export: `export function Footer()`

**page.tsx restructure:**
Replace the current page.tsx content with a composition of landing page sections:
- Keep `export const dynamic = 'force-dynamic';` at top
- Keep the outer `<div className="flex min-h-screen flex-col bg-background">`
- Render `<LandingHeader />` (already imported)
- Render `<HeroSection />` (new import from `@/components/landing/hero-section`)
- Add a comment placeholder: `{/* Value props and testimonials sections added in 37-02 */}`
- Render `<Footer />` (new import from `@/components/landing/footer`)
- Remove ALL inline hero content (Logo, h1, p, CTA div) — that's now in HeroSection
- Remove direct imports of Logo, StartWorkshopButton, SignedIn, Button from page.tsx since they moved to HeroSection
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no type errors. Run `npm run lint` to check for lint issues. Verify page.tsx imports LandingHeader, HeroSection, and Footer. Verify landing-header.tsx has `sticky top-0` in its className.
  </verify>
  <done>Landing page renders with sticky header that persists on scroll, full-height hero section, and footer. Page.tsx is a clean composition of section components. No TypeScript or lint errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run lint` passes
- `npm run build` completes successfully
- Landing page at `/` shows: sticky header with sign-in CTA, full-height hero with headline/subheadline/CTA, footer with branding
- Header stays visible when scrolling (though there is not much to scroll yet — will be testable after 37-02 adds sections)
</verification>

<success_criteria>
- HeroSection component exists and renders headline, subheadline, CTA buttons
- LandingHeader is sticky with backdrop blur
- Footer component exists with branding and copyright
- page.tsx is a clean composition of LandingHeader + HeroSection + Footer
- All theme tokens used (no hardcoded gray/white/blue colors)
- TypeScript and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/37-landing-page/37-01-SUMMARY.md`
</output>
