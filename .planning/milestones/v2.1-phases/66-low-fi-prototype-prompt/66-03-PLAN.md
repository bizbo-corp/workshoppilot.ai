---
phase: 66-low-fi-prototype-prompt
plan: 03
type: execute
wave: 3
depends_on: ["66-01", "66-02"]
files_modified: []
autonomous: false
requirements: [PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04, PROMPT-05]

must_haves:
  truths:
    - "The full pipeline works end-to-end: validate-page gated link → prototype prompt page → Generate → preview → copy"
    - "The generated prompt opens with the wireframe preamble and contains every real Journey Flow screen plus navigation"
    - "Build, typecheck, and lint all pass with the Phase 66 changes"
    - "No file in the new flow references create-v0-chat, deploy-v0, or old-mapper journey-v0-prompt types"
  artifacts:
    - path: "src/app/api/build-pack/generate-prototype-prompt/route.ts"
      provides: "Generation route verified working against a real workshop"
    - path: "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/prototype-prompt-content.tsx"
      provides: "Builder UI verified by human checkpoint"
  key_links:
    - from: "validate step guidance card"
      to: "/workshop/[sessionId]/outputs/prototype-prompt"
      via: "Phase 65 gated link (activates after Journey Flow marked complete)"
      pattern: "outputs/prototype-prompt"
---

<objective>
Verify Phase 66 end-to-end: automated gates (typecheck, lint, build, anti-pattern greps) then a human checkpoint walking the full digital-output path from the validate step through prompt generation and copy.

Purpose: Phase goal is outcome-shaped — "user can generate and copy an agent-agnostic low-fi prototype prompt." This plan proves it on a real workshop before the phase closes.
Output: Verified phase; any gaps recorded for gap-closure planning.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-low-fi-prototype-prompt/66-CONTEXT.md
@.planning/phases/66-low-fi-prototype-prompt/66-01-SUMMARY.md
@.planning/phases/66-low-fi-prototype-prompt/66-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gates — typecheck, lint, build, anti-pattern greps</name>
  <files></files>
  <action>
Run the automated verification suite over the Phase 66 surface:

1. `npx tsc --noEmit` — must pass clean.
2. `npm run lint` — must pass (no new warnings in Phase 66 files).
3. `npm run build` — must complete (catches Next.js route/page issues tsc misses, e.g. async params typing, client/server boundary errors).
4. Anti-pattern greps (all must return empty):
   - `grep -rn "create-v0-chat\|deploy-v0" src/lib/journey-flow/prompt-builder.ts src/lib/ai/prompts/low-fi-prototype-prompt.ts src/app/api/build-pack/generate-prototype-prompt/ "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/"`
   - `grep -rn "journey-v0-prompt\|JourneyMapperState\|journey-mapper/types" src/lib/journey-flow/prompt-builder.ts src/lib/ai/prompts/low-fi-prototype-prompt.ts src/app/api/build-pack/generate-prototype-prompt/ "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/"`
5. Positive greps (must hit):
   - `grep -n "isAnnotation" src/lib/journey-flow/prompt-builder.ts` (annotation filter present)
   - `grep -n "buildLowFiPreamble" src/lib/ai/prompts/low-fi-prototype-prompt.ts src/app/api/build-pack/generate-prototype-prompt/route.ts 2>/dev/null || grep -rn "assembleLowFiPrompt" src/app/api/build-pack/generate-prototype-prompt/route.ts` (preamble deterministically assembled into final prompt)
   - `grep -rn "generatedFromFlowUpdatedAt" "src/app/(dashboard)/workshop/[sessionId]/outputs/prototype-prompt/page.tsx"` (staleness wired)

If any gate fails, fix the issue in the owning file before proceeding to the checkpoint (small fixes in-plan; structural problems → report as gap).

IMPORTANT: per CLAUDE.md multi-worktree rules, confirm which worktree the dev server runs from before the checkpoint; this phase's code lives in the wt2 worktree.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>tsc, lint, and build pass; anti-pattern greps empty; positive greps confirm preamble assembly, annotation filtering, and staleness wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full prototype-prompt flow</name>
  <files></files>
  <action>
Present the checkpoint below to the user and pause. What was built: the Phase 66 low-fi prototype prompt builder — shared journey-understanding module, Gemini generation route with deterministic fallback, and the builder page (fidelity switch, generate, preview, copy, help dialog, staleness).
  </action>
  <what-built>Phase 66 low-fi prototype prompt builder: shared journey-understanding module, Gemini generation route with deterministic fallback, and the builder page (fidelity switch, generate, preview, copy, help dialog, staleness).</what-built>
  <how-to-verify>
Using a workshop that has a completed Journey Flow (e.g. the Phase 64/65 test workshop), with the dev server running from the wt2 worktree (`PORT=3001 npm run dev` if main is on 3000):

1. **Gated entry:** Open the Validate step (step 10). Confirm the guidance card's prototype builder link is active (Journey Flow marked complete) and navigates to `/workshop/[sessionId]/outputs/prototype-prompt?from=validate` with a working "Back to Validation Plan" link.
2. **Fidelity switch (PROMPT-01):** "Low fidelity" shows active; "High fidelity" shows a "Coming later" badge, looks muted, shows a not-allowed cursor, and does nothing when clicked.
3. **Generate (PROMPT-02):** Click "Generate prompt". A loading state appears; within ~15s a prompt renders as a collapsed ~10-line preview. Expand it and confirm: it OPENS with the wireframe rules (grayscale, boxes, no branding, system font), names every screen from your Journey Flow (and NO "Two-Sided Product" annotation as a screen), includes a navigation section, and reads as self-contained plain text any coding agent could execute.
4. **Copy (PROMPT-03):** With the preview COLLAPSED, click "Copy prompt" — button morphs to "Copied ✓" and a toast fires. Paste into a text editor and confirm the FULL prompt was copied, not just the visible lines. Confirm the instruction line reads "Paste into your preferred AI coding agent — v0, Claude, Codex, Replit" and the help link opens a dialog with the 3-step how-to and links to v0/Claude/Codex/Replit. Confirm there is NO "Create on v0" button anywhere.
5. **Persistence + staleness:** Reload the page — the prompt reappears instantly (no AI call). Open Journey Flow, edit any node (e.g. rename), wait ~5s for autosave, return to the prototype prompt page — a "Journey Flow has changed since this prompt was generated" notice with a Regenerate button appears. Regenerate and confirm the notice clears and the prompt updates.
6. **Single-feature scope (PROMPT-05, if you have or can make a feature-scoped flow):** Regenerate the Journey Flow in "Test a single feature" mode, mark it complete, regenerate the prompt — the card shows "Prompt covers your feature mini-flow" and the prompt covers only the mini-flow screens with a single-feature scope statement.
7. **Optional smoke test:** Paste the copied prompt into v0 or Claude and confirm it produces a grayscale wireframe-style prototype.
  </how-to-verify>
  <verify>User responds "approved" after walking the steps above</verify>
  <done>User confirms PROMPT-01 through PROMPT-05 behavior works end-to-end on a real workshop, or lists issues for gap closure</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All automated gates from Task 1 pass.
- Human checkpoint approved covering PROMPT-01 through PROMPT-05 observable behavior.
</verification>

<success_criteria>
- A digital-output workshop can go validate card → prototype prompt page → generate → copy, producing a self-contained wireframe-enforcing prompt scoped to the chosen test scope.
- No v0-specific or old-mapper dependencies exist in the new flow.
</success_criteria>

<output>
After completion, create `.planning/phases/66-low-fi-prototype-prompt/66-03-SUMMARY.md`
</output>
