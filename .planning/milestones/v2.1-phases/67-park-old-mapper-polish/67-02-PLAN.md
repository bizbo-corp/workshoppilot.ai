---
phase: 67-park-old-mapper-polish
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified: []
autonomous: false
requirements: [PARK-01, PARK-02]

must_haves:
  truths:
    - "Production build succeeds with the Phase 67 changes in place"
    - "Old mapper loads without errors at /outputs/journey-map with banner visible; dismiss works; new tab re-shows it"
    - "Full digital-output pipeline works end-to-end: Validate guidance card → Journey Flow (AI baseline → edit → mark complete) → prototype prompt → copy"
    - "Sidebar and Build Pack hub navigation land on Journey Flow, never the old mapper"
  artifacts:
    - path: ".planning/phases/67-park-old-mapper-polish/67-02-SUMMARY.md"
      provides: "Walkthrough results + triage record (inline fixes vs deferred items)"
  key_links:
    - from: "ValidationGuidanceCard"
      to: "/outputs/journey-flow?from=validate"
      via: "Open Journey Flow button (Phase 65 wiring)"
      pattern: "journey-flow\\?from=validate"
    - from: "Journey Flow toolbar"
      to: "/outputs/prototype-prompt"
      via: "prototype link enabled after journeyFlowApproved"
      pattern: "prototype-prompt"
---

<objective>
Verify the full digital-output pipeline end-to-end via automated gates plus a manual human-checkpoint walkthrough, and fix small blockers inline (copy, spacing, broken links). Structural issues get deferred to the backlog, not fixed here.

Purpose: Phase 67's polish scope is E2E path fixes only — confirm the pipeline shipped across Phases 63-66 plus the Plan 01 de-link/banner work holds together as one continuous user path.

Output: Verified pipeline, any small inline fixes committed, walkthrough results recorded in SUMMARY.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-park-old-mapper-polish/67-CONTEXT.md
@.planning/phases/67-park-old-mapper-polish/67-RESEARCH.md
@.planning/phases/67-park-old-mapper-polish/67-01-SUMMARY.md

Triage rule (locked decision): during the walkthrough, small fixes — copy tweaks, spacing, broken links — land inline in this phase as additional commits. Anything structural (new components, flow changes, schema work) is deferred: record it in the SUMMARY's deferred list and STATE.md pending todos. The old mapper is frozen — if a walkthrough issue lives in old-mapper code beyond the banner, defer it; do not patch old mapper internals.

Pipeline route map (for the walkthrough):
1. Validate step: `/workshop/[sessionId]/step/validate` → ValidationGuidanceCard (digital type) → "Open Journey Flow" → `/outputs/journey-flow?from=validate`
2. Journey Flow: scope chooser → AI baseline generation → edit a node → "Mark complete" (sets approved state)
3. Journey Flow toolbar → prototype link (enabled only after approval) → `/outputs/prototype-prompt?from=validate`
4. Prototype Prompt page: generate prompt → copy to clipboard → back-link returns to Validation Plan
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated gates and link audit</name>
  <files></files>
  <action>
Run the full automated gate suite against the Plan 01 changes:

1. `npx tsc --noEmit`
2. `npm run lint`
3. `npm run build`
4. Link audit — confirm no primary-nav/guidance surface still points at the old mapper:
   - `grep -rn "outputs/journey-map" src/ --include="*.ts" --include="*.tsx"` — every remaining hit must be inside `src/app/(dashboard)/workshop/[sessionId]/outputs/journey-map/` itself (self-references) or old-mapper API/lib code (frozen). Hits in sidebar, outputs-content, validate components, or AI prompts are failures.
   - `grep -rn "journey-flow" src/components/layout/dashboard-sidebar.tsx "src/app/(dashboard)/workshop/[sessionId]/outputs/outputs-content.tsx"` — both must hit.
5. Start the dev server check: confirm `npm run dev` boots cleanly (no module resolution errors), then stop it. If a worktree fleet server is already running for this branch, skip the boot check and note which port it serves.

Fix any failures these gates surface before proceeding to the checkpoint. If a fix touches files, add them to the SUMMARY's files-changed list.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <done>tsc, lint, and build all pass; link audit confirms old-mapper hrefs exist only inside the frozen journey-map route/lib/API code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual E2E pipeline walkthrough</name>
  <action>Present the walkthrough below to the user and pause. When the user reports issues, apply the triage rule: fix small issues (copy, spacing, broken links) inline with phase-tagged commits and ask the user to re-verify; record structural issues as deferred in the SUMMARY and STATE.md pending todos. Never patch old-mapper internals beyond the banner. Repeat until the user types "approved".</action>
  <what-built>Phase 67 parked the old UX Journey Mapper: replacement banner on the old page, sidebar + Build Pack hub now link to Journey Flow, and the full digital-output pipeline (Phases 63-66) is wired end-to-end.</what-built>
  <how-to-verify>
Use a workshop with completed Step 9 concept data (seed one with `npm run db:seed:workshop` if needed — Journey Flow AI baseline needs concept data, a blank workshop only shows empty states). Dev server on this worktree (wt2): `PORT=3001 npm run dev` if not already running.

**A. Old mapper parking (PARK-01/PARK-02):**
1. Visit `/workshop/[sessionId]/outputs/journey-map` directly — page loads without errors, full-width banner at top reads "The UX Journey Mapper has been replaced by Journey Flow. Your work here is preserved." with an "Open Journey Flow" link; canvas works normally below it
2. Click "Open Journey Flow" — lands on `/outputs/journey-flow`
3. Go back, click the banner X — banner disappears; reload the page — still hidden; open the same URL in a NEW tab — banner reappears
4. Check the dashboard sidebar — Build Pack section shows "Journey Flow" (workflow icon), clicking it goes to `/outputs/journey-flow`; no "UX Journey Map" entry
5. Visit the Build Pack hub (`/outputs`) — the Journey Flow card shows an "Open Journey Flow" button that navigates to Journey Flow (no Generate button on that card)
6. Toggle dark mode — banner looks right in both themes

**B. Full pipeline E2E:**
7. Go to the Validate step (`/step/validate`) — guidance card (digital output type) shows "Open Journey Flow"; click it — lands on `/outputs/journey-flow?from=validate`
8. In Journey Flow: generate the AI baseline (scope chooser → generate), edit a node (name or description), confirm the edit survives a reload (autosave), then click "Mark complete"
9. The prototype prompt link in the Journey Flow toolbar activates — click through to `/outputs/prototype-prompt`
10. Generate the prototype prompt, click copy — clipboard contains the prompt
11. Use the back-link — returns to the Validation Plan

**Triage as you go:** report anything that blocks or jars. Small issues (copy, spacing, broken links) will be fixed inline this phase; structural issues get deferred to the backlog.
  </how-to-verify>
  <resume-signal>Type "approved" if the full walkthrough passes, or describe each issue found (small issues will be fixed inline and re-verified; structural ones recorded as deferred)</resume-signal>
  <verify>
    <automated>MISSING — human checkpoint; approval signal is the gate. Automated gates already ran in Task 1.</automated>
  </verify>
  <done>User types "approved" after the full A+B walkthrough passes, including any inline-fix re-verification rounds.</done>
</task>

</tasks>

<verification>
- All automated gates green (tsc, lint, build)
- Human checkpoint approved after walkthrough (including any inline-fix iterations)
- Any inline fixes committed with phase-tagged messages; deferred items recorded in SUMMARY and STATE.md pending todos
- No changes to old-mapper internals beyond the Plan 01 banner
</verification>

<success_criteria>
- ROADMAP Phase 67 success criteria all observed true: old mapper loads intact with banner, primary nav/guidance points exclusively to Journey Flow, and a digital-output workshop completes validate card → Journey Flow → mark complete → prototype prompt → copy without blockers
</success_criteria>

<output>
After completion, create `.planning/phases/67-park-old-mapper-polish/67-02-SUMMARY.md` including the walkthrough result and the triage record (inline fixes applied / items deferred).
</output>
