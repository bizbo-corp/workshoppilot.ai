---
phase: 65-validation-guidance-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/workshop/[sessionId]/step/[stepId]/page.tsx
  - src/components/workshop/step-container.tsx
  - src/components/workshop/validate/ValidatePanel.tsx
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "At Validate-step SSR, the page reads the Journey Flow build pack row ('Journey Flow:%' title prefix) and derives journeyFlowApproved from its isApproved field"
    - "journeyFlowApproved reaches ValidatePanel through the same prop chain in BOTH the solo and multiplayer render branches"
    - "No reference to journeyMapApproved remains anywhere in src/ — the old-mapper approval is no longer a gate"
  artifacts:
    - path: "src/app/workshop/[sessionId]/step/[stepId]/page.tsx"
      provides: "SSR query of build_packs with 'Journey Flow:%' prefix → journeyFlowApproved boolean"
      contains: "Journey Flow:%"
    - path: "src/components/workshop/step-container.tsx"
      provides: "journeyFlowApproved prop on StepContainerProps, passed to ValidatePanel in renderStep10Content"
      contains: "journeyFlowApproved"
    - path: "src/components/workshop/validate/ValidatePanel.tsx"
      provides: "journeyFlowApproved on ValidatePanelProps (consumed by plan 03)"
      contains: "journeyFlowApproved"
  key_links:
    - from: "src/app/workshop/[sessionId]/step/[stepId]/page.tsx"
      to: "build_packs"
      via: "drizzle like() on title prefix"
      pattern: "Journey Flow:%"
    - from: "src/components/workshop/step-container.tsx"
      to: "src/components/workshop/validate/ValidatePanel.tsx"
      via: "journeyFlowApproved prop in renderStep10Content"
      pattern: "journeyFlowApproved=\\{journeyFlowApproved\\}"
---

<objective>
Rename the scaffolded-but-unused `journeyMapApproved` prop chain to `journeyFlowApproved` and repoint its SSR source from the old mapper's `'Journey Map:%'` build-pack row to Phase 63's `'Journey Flow:%'` row.

Purpose: VAL-01's prototype-link gating needs the Journey Flow `isApproved` state available at the Validate step. The exact SSR→StepContainer→ValidatePanel plumbing already exists for the old mapper but was never consumed; renaming it (research recommendation, confirmed in planning context: YES, 3 files) avoids a confusing dead prop shadowed by a new one.
Output: `journeyFlowApproved` flowing page → StepContainer (solo + multiplayer branches) → ValidatePanel props, ready for plan 03 to render against.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-validation-guidance-wiring/65-RESEARCH.md

<interfaces>
All four current `journeyMapApproved` sites (exhaustive — confirmed by grep):

1. `src/app/workshop/[sessionId]/step/[stepId]/page.tsx` lines 1173–1187 — SSR query:
```typescript
// Validate step: check if journey map has been approved (gates prototype generation)
let journeyMapApproved = false;
if (step.id === 'validate') {
  const jmRows = await db
    .select({ content: buildPacks.content })
    .from(buildPacks)
    .where(and(eq(buildPacks.workshopId, session.workshop.id), like(buildPacks.title, 'Journey Map:%'), eq(buildPacks.formatType, 'json')))
    .limit(1);
  if (jmRows[0]?.content) {
    try {
      const state = JSON.parse(jmRows[0].content);
      journeyMapApproved = state.isApproved === true;
    } catch { /* invalid JSON */ }
  }
}
```
…and prop passes at line 1281 (multiplayer StepContainer) and line 1315 (solo StepContainer): `journeyMapApproved={journeyMapApproved}`.

2. `src/components/workshop/step-container.tsx` line 160 (`journeyMapApproved?: boolean;` in StepContainerProps), line 213 (destructure `journeyMapApproved = false,`), line 1298 (pass to ValidatePanel inside `renderStep10Content()`).

3. `src/components/workshop/validate/ValidatePanel.tsx` line 70 (`journeyMapApproved?: boolean;` in ValidatePanelProps — declared, never destructured/used).

Phase 63 persistence fact: Journey Flow state is stored in `build_packs.content` (JSON) under title prefix `'Journey Flow:'` with `isApproved: boolean` (set by the toolbar's Mark complete). The old mapper uses `'Journey Map:'` — a deliberately distinct prefix (Phase 63 decision log); do not touch old-mapper rows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint SSR approval query to 'Journey Flow:%' and rename through page.tsx</name>
  <files>src/app/workshop/[sessionId]/step/[stepId]/page.tsx</files>
  <action>
In `src/app/workshop/[sessionId]/step/[stepId]/page.tsx`:

1. In the SSR block at lines 1173–1187: rename `journeyMapApproved` → `journeyFlowApproved`, rename local `jmRows` → `jfRows`, and change the like() prefix from `'Journey Map:%'` to `'Journey Flow:%'`. Update the comment to: `// Validate step: check if the Journey Flow has been marked complete (gates the low-fi prototype builder link)`. Keep everything else identical — same `formatType: 'json'` filter, same try/catch JSON.parse, same `state.isApproved === true` check.

2. Update BOTH StepContainer call sites (multiplayer branch ~line 1281, solo branch ~line 1315) to `journeyFlowApproved={journeyFlowApproved}`. Missing the multiplayer branch is the likely failure mode — verify both.
  </action>
  <verify>
    <automated>cd /Users/michaelchristie/devProjects/workshoppilot.ai/workshoppilot.ai-wt2 && grep -q "Journey Flow:%" "src/app/workshop/[sessionId]/step/[stepId]/page.tsx" && ! grep -q "Journey Map:%" "src/app/workshop/[sessionId]/step/[stepId]/page.tsx" && test "$(grep -c 'journeyFlowApproved={journeyFlowApproved}' 'src/app/workshop/[sessionId]/step/[stepId]/page.tsx')" -eq 2</automated>
  </verify>
  <done>SSR derives journeyFlowApproved from the 'Journey Flow:%' build-pack row and passes it to StepContainer in both solo and multiplayer branches; no 'Journey Map:%' query remains in page.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Rename prop through StepContainer and ValidatePanel interfaces</name>
  <files>src/components/workshop/step-container.tsx, src/components/workshop/validate/ValidatePanel.tsx</files>
  <action>
1. `src/components/workshop/step-container.tsx`: rename all three occurrences — StepContainerProps field (line 160) to `journeyFlowApproved?: boolean;`, destructure default (line 213) to `journeyFlowApproved = false,`, and the ValidatePanel pass in `renderStep10Content()` (line 1298) to `journeyFlowApproved={journeyFlowApproved}`. Also fix the stale comment above `renderStep10Content` ("journey map first, then prototype") to reference Journey Flow.

2. `src/components/workshop/validate/ValidatePanel.tsx`: rename the ValidatePanelProps field (line 70) to `journeyFlowApproved?: boolean;`. Do NOT add it to the function's destructuring or use it in render — plan 03 owns ValidatePanel's render wiring; this plan only fixes the contract so the chain typechecks end to end.

3. Run a repo-wide grep to confirm zero remaining `journeyMapApproved` references in src/.
  </action>
  <verify>
    <automated>cd /Users/michaelchristie/devProjects/workshoppilot.ai/workshoppilot.ai-wt2 && npx tsc --noEmit && ! grep -rq "journeyMapApproved" src/ && grep -q "journeyFlowApproved?: boolean" src/components/workshop/validate/ValidatePanel.tsx && grep -q "journeyFlowApproved={journeyFlowApproved}" src/components/workshop/step-container.tsx</automated>
  </verify>
  <done>journeyFlowApproved flows page → StepContainer → ValidatePanel props; zero journeyMapApproved references remain in src/; tsc passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `! grep -rq "journeyMapApproved" src/` exits 0 (rename is total)
- `grep -rq "Journey Flow:%" "src/app/workshop/[sessionId]/step/[stepId]/page.tsx"` exits 0
- Both StepContainer call sites in page.tsx pass the renamed prop (count = 2)
</verification>

<success_criteria>
- The Validate step's SSR load derives Journey Flow completion from the Phase 63 build-pack row and threads it as `journeyFlowApproved` down to ValidatePanel's prop interface in both solo and multiplayer modes — the gating data VAL-01 needs is in place for plan 03's UI
</success_criteria>

<output>
After completion, create `.planning/phases/65-validation-guidance-wiring/65-02-SUMMARY.md`
</output>
