---
phase: 30-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/workshop/step-container.tsx
  - src/components/workshop/ideation-sub-step-container.tsx
  - src/components/workshop/right-panel.tsx
autonomous: true
must_haves:
  truths:
    - "Canvas panels have visible borders separating them from surrounding UI"
    - "Resizable panel separators show a grip handle on hover for discoverability"
  artifacts:
    - path: "src/components/workshop/step-container.tsx"
      provides: "Main step layout with bordered panels and grip handle separators"
      contains: "GripVertical"
    - path: "src/components/workshop/ideation-sub-step-container.tsx"
      provides: "Ideation layout with bordered panels and grip handle separators"
      contains: "GripVertical"
    - path: "src/components/workshop/right-panel.tsx"
      provides: "Right panel with visible border between canvas and output"
      contains: "border"
  key_links:
    - from: "src/components/workshop/step-container.tsx"
      to: "react-resizable-panels"
      via: "Separator component with grip handle children"
      pattern: "Separator.*GripVertical"
---

<objective>
Add visible panel borders and grip handles to resizable panel separators.

Purpose: Users cannot tell where panels begin/end and don't discover that panels are resizable. Visible borders and grip handles on hover make the split-screen layout obvious and interactive.
Output: All resizable panel separators show a visible border line and display a centered grip handle icon on hover.
</objective>

<execution_context>
@/Users/michaelchristie/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michaelchristie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/workshop/step-container.tsx
@src/components/workshop/ideation-sub-step-container.tsx
@src/components/workshop/right-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add visible borders to all panel separators</name>
  <files>src/components/workshop/step-container.tsx, src/components/workshop/ideation-sub-step-container.tsx, src/components/workshop/right-panel.tsx</files>
  <action>
Fix UX-03 (visible panel borders):

Currently, the Separator in step-container.tsx (line 376) uses `w-0` making it invisible until hover. The ideation-sub-step-container.tsx separators (lines 360, 428, 471) use `w-px bg-border` which is slightly better but still very subtle.

Update ALL horizontal Separators (between chat and canvas/output) to have a consistently visible border:

1. **step-container.tsx** (line 376): Change the Separator from:
```
<Separator className="group relative w-0 hover:w-px data-[resize-handle-state=drag]:w-px">
```
to:
```
<Separator className="group relative w-px bg-border">
```
Remove the inner `<div>` elements that implemented the invisible hit area and hover-only line, since the Separator itself will now be visible. The react-resizable-panels library already provides a built-in hit area.

2. **ideation-sub-step-container.tsx** (lines 360, 428, 471): These already have `w-px bg-border` which is good. Keep them. Remove inner invisible hit area `<div>` elements since the library handles this.

3. **right-panel.tsx** (line 112): The vertical Separator between canvas and output already has `h-px bg-border hover:bg-ring` which is correct. Keep as-is.

The key change is making the step-container.tsx horizontal separator always visible (w-px bg-border) instead of invisible (w-0) by default.
  </action>
  <verify>
Run `npm run build` to verify no build errors. Visual check: borders between chat panel and canvas should be visible as a thin grey line at all times, not only on hover.
  </verify>
  <done>
All panel separators show a visible thin border line (1px, border color) separating panels from surrounding UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add grip handle icon to panel separators on hover</name>
  <files>src/components/workshop/step-container.tsx, src/components/workshop/ideation-sub-step-container.tsx, src/components/workshop/right-panel.tsx</files>
  <action>
Fix UX-04 (grip handle on hover):

Add a centered grip handle icon that appears on hover for all resizable Separator components. This signals to users that the separator is draggable.

1. Import `GripVertical` from `lucide-react` in `step-container.tsx` and `ideation-sub-step-container.tsx`.
2. Import `GripHorizontal` from `lucide-react` in `right-panel.tsx` (for the vertical/horizontal separator between canvas and output).

3. For ALL horizontal Separators (chat|canvas split), add a grip handle as a child:
```tsx
<Separator className="group relative w-px bg-border hover:bg-ring data-[resize-handle-state=drag]:bg-ring">
  <div className="absolute inset-y-0 -left-1 -right-1 cursor-col-resize" />
  <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 group-data-[resize-handle-state=drag]:opacity-100 transition-opacity">
    <div className="flex h-6 w-4 items-center justify-center rounded-sm bg-border">
      <GripVertical className="h-3 w-3 text-muted-foreground" />
    </div>
  </div>
</Separator>
```

The invisible hit area div (-left-1 -right-1) ensures the grip target is at least ~10px wide for easy clicking/touching. The grip icon container uses a small rounded pill shape (h-6 w-4) centered vertically on the separator.

4. For the vertical Separator in `right-panel.tsx` (canvas/output split, line 112), update to:
```tsx
<Separator className="group relative h-px bg-border hover:bg-ring data-[resize-handle-state=drag]:bg-ring">
  <div className="absolute inset-x-0 -top-1 -bottom-1 cursor-row-resize" />
  <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 group-data-[resize-handle-state=drag]:opacity-100 transition-opacity">
    <div className="flex h-4 w-6 items-center justify-center rounded-sm bg-border">
      <GripHorizontal className="h-3 w-3 text-muted-foreground" />
    </div>
  </div>
</Separator>
```

Apply the same pattern to ALL Separator instances across these files:
- step-container.tsx: 1 horizontal Separator
- ideation-sub-step-container.tsx: 3 horizontal Separators (mind-mapping, crazy-eights, idea-selection tabs)
- right-panel.tsx: 1 vertical Separator
  </action>
  <verify>
1. Run `npm run build` to verify no build errors (GripVertical/GripHorizontal imports, JSX structure)
2. Visual check: hover over the separator between chat and canvas -- a small grip handle icon should appear centered on the separator line
3. Visual check: the grip handle should disappear when not hovering
  </verify>
  <done>
All resizable panel separators show a centered grip handle icon (GripVertical for horizontal splits, GripHorizontal for vertical splits) on hover, signaling draggability to users.
  </done>
</task>

</tasks>

<verification>
Both panel UX requirements addressed:
- UX-03: Panel borders always visible (1px border-color line)
- UX-04: Grip handle appears on hover (GripVertical/GripHorizontal icon in pill container)
</verification>

<success_criteria>
1. Border line between chat panel and canvas/output panel is visible at all times (not just on hover)
2. Hovering over any panel separator reveals a centered grip handle icon
3. Grip handle icon disappears when mouse leaves the separator area
4. Separators remain draggable with smooth resize behavior
5. No TypeScript or build errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/30-ux-polish/30-02-SUMMARY.md`
</output>
